pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id("io.quarkus") version "2.5.1.Final"
    }
}

rootProject.name = "ccnl-christmas-challenge"
